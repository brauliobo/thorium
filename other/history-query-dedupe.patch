diff --git a/components/history/core/browser/visit_database.cc b/components/history/core/browser/visit_database.cc
index 73bd519289c2d..6e55f50ae16b8 100644
--- a/components/history/core/browser/visit_database.cc
+++ b/components/history/core/browser/visit_database.cc
@@ -823,6 +823,145 @@ GetAllAppIdsResult VisitDatabase::GetAllAppIds() {
 bool VisitDatabase::GetVisibleVisitsInRange(const QueryOptions& options,
                                             VisitVector* visits) {
   visits->clear();
+
+// TODO(crbug.com/457641486) Clean up preprocessor statements once feature is
+// rolled out.
+#if !BUILDFLAG(IS_IOS)
+  const bool actor_visit_filtering_enabled =
+      history::IsBrowsingHistoryActorIntegrationM3Enabled();
+#else
+  const bool actor_visit_filtering_enabled = false;
+#endif
+
+  // For the common duplicate-removal queries, let SQLite reduce each duplicate
+  // group to the first matching visit before returning rows to C++. Chromium's
+  // bundled SQLite omits window functions, so use grouped aggregates here.
+  const bool rank_duplicate_visits_in_sql =
+      !actor_visit_filtering_enabled &&
+      (options.duplicate_policy == QueryOptions::REMOVE_ALL_DUPLICATES ||
+       options.duplicate_policy == QueryOptions::REMOVE_DUPLICATES_PER_DAY);
+  if (rank_duplicate_visits_in_sql) {
+    std::string joins;
+    std::vector<std::string> where_clauses;
+    std::vector<int64_t> binding_values;
+
+    if (options.policy_for_404_visits == VisitQuery404sPolicy::kExclude404s) {
+      joins +=
+          " LEFT OUTER JOIN context_annotations ON visits.id = "
+          "context_annotations.visit_id";
+      where_clauses.push_back(
+          "(context_annotations.response_code IS NULL OR "
+          "context_annotations.response_code != 404)");
+    }
+
+    if (options.visit_order == QueryOptions::RECENT_FIRST) {
+      where_clauses.push_back("visits.visit_time >= ?");
+      where_clauses.push_back("visits.visit_time < ?");
+    } else {
+      where_clauses.push_back("visits.visit_time > ?");
+      where_clauses.push_back("visits.visit_time <= ?");
+    }
+    binding_values.push_back(options.EffectiveBeginTime());
+    binding_values.push_back(options.EffectiveEndTime());
+
+    if (options.app_id) {
+      where_clauses.push_back("visits.app_id = ?");
+    }
+
+    // Same visibility policy as TransitionIsVisible(), expressed in SQL so
+    // duplicate ranking does not waste work on invisible subframes/redirects.
+    where_clauses.push_back("(visits.transition & ?) != 0");
+    binding_values.push_back(ui::PAGE_TRANSITION_CHAIN_END);
+    where_clauses.push_back("(visits.transition & ?) NOT IN (?, ?, ?)");
+    binding_values.push_back(ui::PAGE_TRANSITION_CORE_MASK);
+    binding_values.push_back(ui::PAGE_TRANSITION_AUTO_SUBFRAME);
+    binding_values.push_back(ui::PAGE_TRANSITION_MANUAL_SUBFRAME);
+    binding_values.push_back(ui::PAGE_TRANSITION_KEYWORD_GENERATED);
+
+    const std::string where_sql =
+        " WHERE " + base::JoinString(where_clauses, " AND ");
+
+    const char* order_sql = options.visit_order == QueryOptions::RECENT_FIRST
+                                ? "visits.visit_time DESC, visits.id DESC"
+                                : "visits.visit_time ASC, visits.id DESC";
+    const char* visit_time_aggregate =
+        options.visit_order == QueryOptions::RECENT_FIRST ? "MAX" : "MIN";
+
+    const bool dedupe_per_day =
+        options.duplicate_policy == QueryOptions::REMOVE_DUPLICATES_PER_DAY;
+    std::string dedupe_columns = "url";
+    if (options.duplicate_policy == QueryOptions::REMOVE_DUPLICATES_PER_DAY) {
+      dedupe_columns += ", dedupe_day";
+    }
+
+    std::string sql = "WITH filtered_visits AS (SELECT" HISTORY_VISIT_ROW_FIELDS;
+    if (dedupe_per_day) {
+      sql +=
+          ", strftime('%Y-%m-%d', "
+          "visits.visit_time / 1000000 - 11644473600, "
+          "'unixepoch', 'localtime') AS dedupe_day";
+    }
+    sql += " FROM visits";
+    sql += joins;
+    sql += where_sql;
+    sql += "), selected_visit_times AS (SELECT ";
+    sql += dedupe_columns;
+    sql += ", ";
+    sql += visit_time_aggregate;
+    sql +=
+        "(visit_time) AS selected_visit_time FROM filtered_visits GROUP BY ";
+    sql += dedupe_columns;
+    sql += "), selected_visits AS (SELECT filtered_visits.url";
+    if (dedupe_per_day) {
+      sql += ", filtered_visits.dedupe_day";
+    }
+    sql +=
+        ", MAX(filtered_visits.id) AS id FROM filtered_visits JOIN "
+        "selected_visit_times ON selected_visit_times.url = filtered_visits.url ";
+    if (dedupe_per_day) {
+      sql +=
+          "AND selected_visit_times.dedupe_day = filtered_visits.dedupe_day ";
+    }
+    sql +=
+        "AND selected_visit_times.selected_visit_time = "
+        "filtered_visits.visit_time "
+        "GROUP BY filtered_visits.url";
+    if (dedupe_per_day) {
+      sql += ", filtered_visits.dedupe_day";
+    }
+    sql += ") SELECT" HISTORY_VISIT_ROW_FIELDS
+           "FROM filtered_visits visits JOIN selected_visits ON "
+           "selected_visits.id = visits.id ORDER BY ";
+    sql += order_sql;
+
+    if (options.max_count > 0) {
+      // Fetch one extra row so FillVisitVectorWithOptions() can report that
+      // more results are available, matching the generic path.
+      sql += " LIMIT ?";
+    }
+
+    sql::Statement statement(
+        GetDB().GetUniqueStatement(base::cstring_view(sql)));
+    if (statement.is_valid()) {
+      int bind_index = 0;
+      // Bind values in the same order as where_clauses: time range, optional
+      // app id, then transition visibility predicates.
+      for (size_t i = 0; i < 2; ++i) {
+        statement.BindInt64(bind_index++, binding_values[i]);
+      }
+      if (options.app_id) {
+        statement.BindString(bind_index++, *options.app_id);
+      }
+      for (size_t i = 2; i < binding_values.size(); ++i) {
+        statement.BindInt64(bind_index++, binding_values[i]);
+      }
+      if (options.max_count > 0) {
+        statement.BindInt(bind_index++, options.max_count + 1);
+      }
+      return FillVisitVectorWithOptions(statement, options, visits);
+    }
+  }
+
   sql::Statement statement;
   if (!PrepareVisibleVisitsQuery(options, std::nullopt, statement)) {
     return false;
